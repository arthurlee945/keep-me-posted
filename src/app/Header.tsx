import { FC } from "react";

interface HeaderProps {}

const Header: FC<HeaderProps> = () => {
    return <header className="w-full p-4 flex items-center justify-between border-b-2 border-neutral-100">HI</header>;
};

export default Header;
