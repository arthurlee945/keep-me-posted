import { FC } from 'react';

interface ProjectsListProps {}

const ProjectsList: FC<ProjectsListProps> = () => {
    return <section className="w-full flex flex-wrap">ProjectsList</section>;
};

export default ProjectsList;
